import { BorderDirective } from './border.directive';
import { ElementRef } from '@angular/core';

describe('BorderDirective', () => {
  it('should create an instance', () => {
    const directive = BorderDirective;
    expect(directive).toBeTruthy();
  });
});
